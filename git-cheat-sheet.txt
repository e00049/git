
git log --oneline --graph --decorate development

git branch   -  Active branch - HEAD

git checkout development  - switch to exitsting branch

git checkout -b uat  - change create 

git merge uat  - uat will be merge with main branch

git branch -d uat

git mv old-file-name new-file-name 

git add file-name              -  from local to staging area 
git reset file-name            -  from staging area to local
git restore --staged file-name -  from staging area to local
git revert commit-id           - Undo commit and create new commit

git reset --soft HEAD^  -  Undo last commit 


git show HEAD~1:file.txt   -  How was the file while commit 1 from top to down 
git show HEAD~2:file.txt > stage-002.txt  - what was the content at log2 stage

git diff HEAD~2 file.txt   - difference between current and from 2nd commit

git restore -s HEAD~1 file.txt  - move to log 01 stage

git help -a     -  we can see all the commands
git help config - To get man page for the respective command 
git config —list —show-origin 
git config —global —unset user.name 
git config —global —remove-section user

git config user.email -  to view the which user’s configured - directory level 
git config —unset user.email - To remove the user from the directiory level
git config user.name - To get user name for directory


git config —local user.email e00049@gmail.com

git config —local —remove-section. 

git mv old_file_name new_file_name 

git log --grep="fix" -  To start to searching string from commit message



GIT Commands:

""" Basic Level commands """

// Push an existing folder 
#1. To initialize a Git repository
    git init 

#2. Configuration    
    git config user.name e00049
    git config user.email e00049@gmail.com

#3. To add remote repo URL
    git remote add origin http://34.31.128.202/devops/django.git

#4. Branch to push your code
    git checkout -b dev

#5. To list of file you want to add
    git add .

#6. git commit with message
    git commit -m "fect: #1. initial commit"    

#7. git push your committed branch
    git push origin dev    

#8. pull the changes from remote repo
    git pull origin dev


// Create a new repository 
#1. git clone 
    git clone http://34.31.128.202/devops/django # while clone the repo you'll be authuticate. 

#2. Configuration    
    git config user.name e00049
    git config user.email e00049@gmail.com

#3. To list of file you want to add
    git add .

#4. git commit with message
    git commit -m "fect: #1. initial commit"    

#5. git push your committed branch
    git push origin dev    

#6. pull the changes from remote repo
    git pull origin dev    

#7. To check status 
    git status 

#8. To check commit histroy 
    git log

#9. To check present branch 
    git branch 

#10. To create new branch and chnage 
    git checkout -b dev



""" Intermeditate Level commands """

#1. rename the file 
    git mv old-name new-name 

#2. delete the file
    git rm file 

#3. Undo the add 
    git reset

#4. Undo the committed 
    git reset --soft HEAD~1

#5. Most recent commit message change
    git commit --amend -m "New commit message"


""" Advance Level commands """
#1. 
    git fetch 

#2.
    git merge

#3. 
    git rebase 

#4. 
    git show            

#5.
    git grep work-to-search

#6. 
    git shortlog
    
            
